import React, { useContext } from 'react'
import { useTimer } from 'react-timer-hook';
import { MyContext } from '../context/MyContext';

function Timer() {

  

    /* const {
        totalSeconds,
        seconds,
        minutes,
        hours,
        days,
        isRunning,
        start,
        pause,
        resume,
        restart,
      } = useTimer({ expiryTimestamp, onExpire:()=> changeQuestion()});
    

      const changeQuestion = () =>{
        
        setTimeout(()=>{
        },500)
        const restartTimeVariable = new Date()
        restartTimeVariable.setSeconds(restartTimeVariable.getSeconds() + 10)
        restart(restartTimeVariable)
    } */

  return (
    <div>
       
    </div>
  )
}

export default Timer